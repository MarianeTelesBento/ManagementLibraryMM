@{
    ViewData["Title"] = "Save To Json Page";
}

<h1 class="display-4">Convert Book List</h1>

<div class="container mt-4" style="display:flex; flex-direction: column; align-items:center; justify-content:center;">
    
    <a href="@Url.Action("Convert", "Book")" class="btn btn-primary btn-sm">Convert</a>

    <a href="@Url.Action("Download", "Book")" class="btn btn-primary btn-sm">Download</a>

</div>

